apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: qsan-csi-pvc-block
spec:
  volumeMode: Block
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 8Gi
  storageClassName: qsan-xevo-storage
