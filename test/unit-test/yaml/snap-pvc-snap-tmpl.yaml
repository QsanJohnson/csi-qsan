apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: testsnap-pvc-snap%d
  namespace: qtest
spec:
  volumeSnapshotClassName: qtest-xevo-snapclass
  source:
    persistentVolumeClaimName: testsnap-pvc