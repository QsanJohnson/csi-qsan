apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: clone-source-pvc-clone%d
  namespace: qtest
spec:
  storageClassName: qtest-xevo-storage
  dataSource:
    name: clone-source-pvc%d
    kind: PersistentVolumeClaim
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10Gi

---
kind: Pod
apiVersion: v1
metadata:
  name: test-clone-pod%d
  namespace: qtest
spec:
  containers:
  - image: busybox
    command: ["sleep", "99999999"]
    name: test-container
    imagePullPolicy: IfNotPresent
    volumeMounts:
    - mountPath: /test-pv
      name: test-pv-storage
  volumes:
    - name: test-pv-storage
      persistentVolumeClaim:
        claimName: clone-source-pvc-clone%d