apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: pvc-test
  namespace: qtest
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 10000000000
  storageClassName: qtest-xevo-storage
