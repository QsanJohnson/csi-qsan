apiVersion: snapshot.storage.k8s.io/v1
kind: VolumeSnapshot
metadata:
  name: restore-source-pvc-snap%d
  namespace: qtest
spec:
  volumeSnapshotClassName: qtest-xevo-snapclass
  source:
    persistentVolumeClaimName: restore-source-pvc